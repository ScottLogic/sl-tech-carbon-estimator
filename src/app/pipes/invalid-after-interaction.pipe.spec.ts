import { InvalidAfterInteractionPipe } from './invalid-after-interaction.pipe';

describe('InvalidAfterInteractionPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidAfterInteractionPipe();
    expect(pipe).toBeTruthy();
  });
});
